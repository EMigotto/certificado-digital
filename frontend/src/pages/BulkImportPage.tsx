/**
 * BulkImportPage — re-exports the full implementation from pages/BulkImport.
 *
 * This file exists as the entry point used by the router (lazy import).
 */

export { BulkImportPage as default } from './BulkImport/BulkImportPage';
